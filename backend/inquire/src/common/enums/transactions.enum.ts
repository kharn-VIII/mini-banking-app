
export enum TransactionType {
    TRANSFER = 'transfer',
    EXCHANGE = 'exchange',
}
  
export enum TransactionStatus {
    PENDING = 'pending',
    COMPLETED = 'completed',
    FAILED = 'failed',
}